/**
 * RAG — Embedding Provider Interface + Factory
 *
 * Re-exports the EmbeddingProvider interface and provides a factory
 * that selects the correct implementation based on available config.
 */

export type { EmbeddingProvider } from '../types/index.js';
