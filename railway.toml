[build]
builder = "nixpacks"
buildCommand = "npm ci --legacy-peer-deps && npm run build --workspace=@vel-ai/shared && npm run build --workspace=@vel-ai/api"

[deploy]
startCommand = "node backend/api/dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
